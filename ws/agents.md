Your task is to code not RUN the environement so Don't run git, python, http servers, etc. also Don't interrupt by asking if you can run commands like PowerShell and such. do what you need to answer correctly to the task.

